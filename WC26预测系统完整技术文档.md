# WC26 世界杯比赛预测系统 — 完整技术文档

> 本文档完整梳理了 WC26 预测系统的所有模型、算法、数据流、权重配置和外部数据源，供量化评分系统优化参考。

---

## 目录

1. [系统架构概览](#1-系统架构概览)
2. [核心模型详解](#2-核心模型详解)
   - [2.1 Dixon-Coles 泊松模型](#21-dixon-coles-泊松模型)
   - [2.2 TabularMatchEnhancer 表格增强器](#22-tabularmatchenhancer-表格增强器)
   - [2.3 κ-Elo Davidson 评分系统](#23-κ-elo-davidson-评分系统)
   - [2.4 Pi-Rating 评分系统](#24-pi-rating-评分系统)
   - [2.5 Weibull Copula 模型](#25-weibull-copula-模型)
3. [概率融合策略 (FusionGraph)](#3-概率融合策略-fusiongraph)
4. [权重配置系统](#4-权重配置系统)
5. [信号调整系统](#5-信号调整系统)
6. [外部数据源](#6-外部数据源)
   - [6.1 市场赔率校准 (MarketCalibrator)](#61-市场赔率校准-marketcalibrator)
   - [6.2 天气数据 (WeatherService)](#62-天气数据-weatherservice)
   - [6.3 伤病数据 (InjuryDataService)](#63-伤病数据-injurydataservice)
   - [6.4 新闻信号 (NewsSignals)](#64-新闻信号-newssignals)
7. [蒙特卡洛锦标赛模拟](#7-蒙特卡洛锦标赛模拟)
8. [评估指标与后处理](#8-评估指标与后处理)
9. [FIFA 排名层级数据](#9-fifa-排名层级数据)
10. [完整数据流总结](#10-完整数据流总结)

---

## 1. 系统架构概览

```
                            ┌──────────────────────────┐
                            │   PredictionPipeline      │
                            │   (预测入口)               │
                            └──────────┬───────────────┘
                                       │
           ┌───────────────────────────┼───────────────────────────┐
           │                           │                           │
           ▼                           ▼                           ▼
   ┌───────────────┐         ┌───────────────┐         ┌───────────────┐
   │ from_artifacts │         │from_snapshot_env│        │predict_sync() │
   │ (离线模式)     │         │ (DB异步模式)    │        │ (同步模式)     │
   └───────┬───────┘         └───────┬───────┘         └───────┬───────┘
           │                         │                         │
           └─────────────────────────┼─────────────────────────┘
                                     │
           ┌─────────────────────────▼─────────────────────────┐
           │            多模型分层预测管道                      │
           │                                                   │
           │   Layer 1: Dixon-Coles → xG + 得分矩阵           │
           │   Layer 2: TabularEnhancer → 特征ML概率           │
           │   Layer 3: Weibull Copula → 相关性调整           │
           │   Layer 4: κ-Elo Davidson → 评分概率             │
           │   Layer 5: Pi-Rating → 净胜球评分概率            │
           │   Layer 6: Market Calibration → 市场共识         │
           │   Layer 7: Signal Adjustments → 信号调整         │
           └───────────────────────┬───────────────────────────┘
                                   │
                                   ▼
                        ┌──────────────────┐
                        │   FusionGraph     │
                        │   顺序加权融合    │
                        └────────┬─────────┘
                                 │
                                 ▼
                        ┌──────────────────┐
                        │  PredictionResult │
                        │  H/D/A 最终概率   │
                        └──────────────────┘
```

**四种预测模式：**

| 模式 | 使用组件 | 描述 |
|------|---------|------|
| `baseline` | DC only | 仅 Dixon-Coles |
| `standard` | DC + Enhancer + Elo | 标准三层 |
| `full` | DC + Enhancer + Elo + Pi | 四层全开 |
| `research-full` | DC + Enhancer + Elo + Pi + Weibull | 五层全开 + 研究 |

---

## 2. 核心模型详解

### 2.1 Dixon-Coles 泊松模型

**文件位置：** `backend/app/services/dixon_coles.py`

**理论基础：**
- Dixon & Coles (1997) — 对独立泊松模型的改进
- 引入低比分相关性参数 `ρ` (rho)，修正 0-0、1-0、0-1、1-1 的低比分概率偏差
- 使用泊松分布对进球数建模

**核心公式：**

**进球期望：**
```
λ_home = attack[home] × defense[away] × exp(home_advantage)   [非中立场地]
μ_away = attack[away] × defense[home]
```

**低比分 τ 修正 (Dixon-Coles correction)：**
```
τ(0,0) = 1 - λ × μ × ρ
τ(1,0) = 1 + μ × ρ
τ(0,1) = 1 + λ × ρ
τ(1,1) = 1 - ρ
τ(x,y) = 1.0    [其他情况]
```

**组合概率：**
```
P(X=x, Y=y) = τ(x,y) × Poisson(x|λ) × Poisson(y|μ)
```

**模型参数：**
- `attack_params`: 每支球队的攻击力参数
- `defense_params`: 每支球队的防守力参数
- `home_advantage`: 主场优势参数
- `rho`: 低比分相关性参数（通过 tanh 约束在 [-1, 1]）

**训练方法：**
- 优化算法: L-BFGS-B
- 负对数似然最小化（向量化实现，30-100倍加速）
- 参数约束: `attack/defense ∈ [exp(log(0.2)), exp(log(5.0))]` 即 `[0.2, 5.0]`
- `home_advantage ∈ [-1, 1]`
- `rho_raw ∈ [-3, 3]`（经 tanh 变换后 rho ∈ [-0.995, 0.995]）

**时间衰减权重：**
```python
weight = exp(-ln(2) × days_diff / 180) × competition_weight
```
半衰期为 180 天。

**贝叶斯收缩先验：**
- 对小样本球队施加 L2 正则化
- `prior_scale = max(1.0, min_matches × 0.5)`
- `shrink_weight = prior_scale / team_count`

**冷启动机制：**
- 针对训练数据少于 5 场的球队
- 使用洲际平均攻击力 × FIFA 层级修正

**FIFA 层级修正系数：**
```
Tier 1 (Top 10):     attack × 1.15,  defense × (2-1.15)=0.85
Tier 2 (11-30):      attack × 1.05,  defense × 0.95
Tier 3 (31-50):      attack × 0.95,  defense × 1.05
Tier 4 (51+):        attack × 0.88,  defense × 1.12
Unknown:             attack × 1.00,  defense × 1.00
```

**输出格式：**
```python
{
    "home_win_prob": float,      # 主胜概率
    "draw_prob": float,          # 平局概率
    "away_win_prob": float,      # 客胜概率
    "home_xg": float,            # 主队期望进球
    "away_xg": float,            # 客队期望进球
    "top3_scores": [             # Top3 比分
        {"score": "1:0", "prob": 0.12}, ...
    ],
    "score_matrix": [[float]],   # 5×5 得分矩阵
    "data_quality": "fitted|estimated_prior",
    "confidence_penalty": float, # 置信度惩罚
    "risk_tags": ["冷启动球队: ..."],
    "cold_start_warnings": [...]
}
```

**评估指标：**
- Brier Score
- Log Loss
- 校准曲线 (10桶)
- 精确比分命中率
- Top3 比分命中率

---

### 2.2 TabularMatchEnhancer 表格增强器

**文件位置：** `backend/app/services/tabular_match_model.py`

**算法：**
- 主算法: `HistGradientBoostingClassifier` (sklearn)
- 备选: `XGBoost` (可选)
- 超参数: `learning_rate=0.06, max_depth=4, max_iter=220, min_samples_leaf=4, random_state=42`

**36维特征向量（完整列表）：**

| 编号 | 特征名 | 描述 |
|------|--------|------|
| 1 | `is_neutral_venue` | 是否中立场地 |
| 2 | `competition_weight` | 比赛权重 |
| 3 | `home_matches_played` | 主队历史比赛数 |
| 4 | `away_matches_played` | 客队历史比赛数 |
| 5 | `experience_gap` | 比赛经验差距 |
| 6 | `home_goals_for_avg` | 主队场均进球 |
| 7 | `home_goals_against_avg` | 主队场均失球 |
| 8 | `away_goals_for_avg` | 客队场均进球 |
| 9 | `away_goals_against_avg` | 客队场均失球 |
| 10 | `goal_balance_gap` | 净胜球差距 |
| 11 | `home_xg_for_avg` | 主队场均 xG |
| 12 | `home_xg_against_avg` | 主队场均 xGA |
| 13 | `away_xg_for_avg` | 客队场均 xG |
| 14 | `away_xg_against_avg` | 客队场均 xGA |
| 15 | `xg_balance_gap` | xG 净差值差距 |
| 16 | `home_recent_goals_for_avg` | 主队近5场场均进球 |
| 17 | `home_recent_goals_against_avg` | 主队近5场场均失球 |
| 18 | `away_recent_goals_for_avg` | 客队近5场场均进球 |
| 19 | `away_recent_goals_against_avg` | 客队近5场场均失球 |
| 20 | `recent_goal_gap` | 近5场净胜球差距 |
| 21 | `home_recent_xg_for_avg` | 主队近5场场均 xG |
| 22 | `home_recent_xg_against_avg` | 主队近5场场均 xGA |
| 23 | `away_recent_xg_for_avg` | 客队近5场场均 xG |
| 24 | `away_recent_xg_against_avg` | 客队近5场场均 xGA |
| 25 | `recent_xg_gap` | 近5场 xG 净差值差距 |
| 26 | `home_points_per_match` | 主队场均积分 |
| 27 | `away_points_per_match` | 客队场均积分 |
| 28 | `points_gap` | 积分差距 |
| 29 | `home_recent_points_per_match` | 主队近5场场均积分 |
| 30 | `away_recent_points_per_match` | 客队近5场场均积分 |
| 31 | `recent_points_gap` | 近5场积分差距 |
| 32 | `home_win_rate` | 主队历史胜率 |
| 33 | `away_win_rate` | 客队历史胜率 |
| 34 | `win_rate_gap` | 胜率差距 |
| 35 | `home_rest_days` | 主队休息天数 |
| 36 | `away_rest_days` | 客队休息天数 |
| 37 | `rest_day_diff` | 休息天数差 |

**特征构建方式：**
- 全量窗口 (expanding window): 所有历史数据取均值
- 近期窗口 (rolling window): 最近 5 场
- 使用 groupby + expanding/rolling 实现 O(n log n) 向量化

**默认填充值（新球队首场比赛）：**
```python
goals_for = goals_against = xg = league_avg (≈1.3)
points_per_match = 1.3
win_rate = 0.33
rest_days = 7.0
```

**输出：** 三分类概率 `[H, D, A]`

---

### 2.3 κ-Elo Davidson 评分系统

**文件位置：** `backend/app/services/elo_ratings.py`

**理论基础：**
- 标准 Elo 评分: Arpad Elo (1960)
- κ-Elo Davidson 平局模型: Szczecinski & Djebbi (2020)
- 来源: https://arxiv.org/abs/1805.05293

**核心参数：**
```python
DEFAULT_RATING = 1500.0        # 初始评分
HOME_ADVANTAGE = 100.0         # 主场优势 Elo 分
K_LEAGUE = 20                  # 联赛 K 因子
K_KNOCKOUT = 32                # 淘汰赛 K 因子
```

**胜负期望公式：**
```
expected_home = 1 / (1 + 10^((R_away - (R_home + H)) / 400))
```

**κ-Elo Davidson 平局概率：**
```
P(draw) = κ × sqrt(σ(r) × σ(-r))
其中:
  r = elo_diff / 400
  σ(r) = 1 / (1 + 10^(-r))
  κ ∈ [0.18, 0.28] (比赛类型相关)
```

**K 因子规则：**
```
competition_weight >= 1.5 (世界杯): K = 32
competition_weight >= 1.2 (欧冠):   K = 28
其他:                               K = 20
```

**κ 值（按比赛类型）：**
```
英超 (EPL):          κ = 0.28 (数据库可配)
欧冠 (UCL):          κ = 0.18
FA Cup:              κ = 0.28
默认:                κ = 0.24
```

**升级公式：**
```
R_new = R_old + K × (result - expected)
result ∈ {1 (胜), 0.5 (平), 0 (负)}  [主队视角]
```

**概率分配：**
```
1. 先计算 p_draw
2. p_home = expected_home × (1 - p_draw)
3. p_away = (1 - expected_home) × (1 - p_draw)
4. 归一化确保 sum = 1.0
```

---

### 2.4 Pi-Rating 评分系统

**文件位置：** `backend/app/services/pi_ratings.py`

**理论基础：** Constantinou & Fenton (2012)

**核心特性：**
- 零中心评分: 正值 = 强于平均，负值 = 弱于平均
- 响应净胜球差：5-0 赢球比 1-0 赢球产生更大的评分变化
- 使用 diminishing returns 防止大比分屠杀主导评分

**参数：**
```python
k = 0.1      # 评分变化速率
PROB_SCALE = 0.35
```

**预测公式：**
```python
home_adj = 0.3 (非中立场地) / 0.0 (中立场地)
xg_diff = (home_rating + home_adj - away_rating) × 0.35 × 2.0

home_win_raw = 1 / (1 + exp(-xg_diff × 2.5))
away_win_raw = 1 / (1 + exp(xg_diff × 2.5))
draw_raw = 0.26 × exp(-xg_diff² / 2.0)    # 高斯衰减
```

**融合方式：**
```python
fused = base × (1 - pi_weight) + pi × pi_weight
```

---

### 2.5 Weibull Copula 模型

**文件位置：** `backend/app/services/weibull_model.py`

**理论基础：** Boshnakov, Kharrat & McHale (2017)

**核心思想：**
- 假设进球间隔时间服从 Weibull 分布 (而非指数/Poisson)
- 捕获比赛中的动量效应 (momentum effects)
- Frank Copula 引入球队间相关性
- 在 over/under 2.5 球预测上优于独立 Poisson

**实现方式：**
- 通过 `penaltyblog` 库的 `WeibullCopulaGoalsModel` 实现
- 可选组件，仅在 `research-full` 模式下运行

**融合方式：**
```python
fused = base × (1 - wb_weight) + wb_pred × wb_weight
```

---

## 3. 概率融合策略 (FusionGraph)

**文件位置：** `backend/app/services/fusion_graph.py`

**融合方式：顺序加权融合**

**标准管道融合顺序：**

```
Step 1: DC + Enhancer 融合
  fused₁ = DC × dc_weight + Enhancer × (1 - dc_weight)    → 归一化

Step 2: 加入 Elo
  fused₂ = fused₁ × (1 - elo_weight) + Elo × elo_weight   → 归一化

Step 3: 加入 Pi-Rating
  fused₃ = fused₂ × (1 - pi_weight) + Pi × pi_weight      → 归一化

Step 4: 加入 Weibull (可选)
  fused₄ = fused₃ × (1 - wb_weight) + Weibull × wb_weight → 归一化
```

**有效权重推导（展开顺序链）：**
```
dc_effective       = dc_weight × (1 - elo_weight) × (1 - pi_weight)
enhancer_effective = (1 - dc_weight) × (1 - elo_weight) × (1 - pi_weight)
elo_effective      = elo_weight × (1 - pi_weight)
pi_effective       = pi_weight
```

**模型分歧度检测：**
- 计算每对模型之间的主胜概率差异
- 报告 `max_home_diff` (最大分歧)
- 分歧 > 30% → 触发 `high_model_disagreement` 风险标签

**每一步都记录到 FusionGraph.steps 用于审计追踪：**
```python
{
    "name": "dc+enhancer",
    "formula": "base_weight=0.55",
    "before": {"dixon_coles": [h,d,a], "enhancer": [h,d,a]},
    "after": [h,d,a]
}
```

---

## 4. 权重配置系统

**文件位置：** `backend/app/services/weights.py`

**权重读取优先级：**
1. 数据库 `model_weight_config` 表中的 RPS 自动优化权重
2. 比赛类型感知的代码默认值

**世界杯默认权重 (V3.8)：**
```python
_WORLD_CUP = WeightConfig(
    dc=0.70,          # Dixon-Coles 基础权重 (↑ 从 0.55)
    enhancer=0.20,    # Enhancer 权重 (↓ 从 0.25)
    elo=0.10,         # Elo 权重 (↑ 从 0.05)
    pi=0.10,          # Pi-Rating 权重 (↑ 从 0.05)
    weibull=0.10,     # Weibull 权重
    market_max=0.25,  # 市场最大融合比例 (↑ 从 0.10, Phase 3)
)
```

**各比赛类型权重对比：**

| 类型 | DC | Enhancer | Elo | Pi | Weibull | Market Max |
|------|-----|----------|-----|-----|---------|-------------|
| World Cup V3.8 | 0.70 | 0.20 | 0.10 | 0.10 | 0.10 | 0.25 |
| UCL Final | 0.42 | 0.30 | 0.08 | 0.12 | 0.08 | 0.08 |
| UCL Knockout | 0.45 | 0.28 | 0.07 | 0.10 | 0.10 | 0.10 |
| League Default | 0.50 | 0.30 | 0.05 | 0.05 | 0.10 | 0.10 |
| Friendly V2.7 | 0.28 | 0.42 | 0.02 | 0.16 | 0.12 | 0.10 |

**友谊赛 V2.7 的自我进化逻辑：**
- 基于 3 场友谊赛回顾数据自动调整
- DC 0/3 正确 → 权重从 0.38 降到 0.28
- Elo 0/3 正确 → 权重从 0.04 降到 0.02
- Enhancer 2/3 → 权重保持 0.42
- Pi 唯一正确预测 SG-CN → 权重从 0.04 升到 0.16

**比赛权重自动检测：**
```python
WORLD_CUP_COMPETITION_WEIGHT = 1.5
FRIENDLY_COMPETITION_WEIGHT = 0.5
DEFAULT_COMPETITION_WEIGHT = 0.9
```

---

## 5. 信号调整系统

**文件位置：** `backend/app/services/signal_adjuster.py` (异步版) 和 `signal_adjuster_sync.py` (同步版)

### 5.1 场地因素调整

**高海拔场地识别：**
```
Estadio Azteca (墨西哥城): 2,240m → 触发高海拔
Estadio Akron (瓜达拉哈拉): 1,560m → 触发高海拔
Estadio BBVA (蒙特雷): 537m → 不触发

高海拔阈值: 1,500m
双方 xG × 0.95
```

### 5.2 手动事件信号处理

**支持信号类型及最大调整幅度：**

| 信号类型 | 最大概率偏移 | 示例 |
|----------|-------------|------|
| `injury` (关键前锋/球员) | 0.15 | 核心前锋伤缺 |
| `injury` (门将) | 0.10 | 首发门将伤缺 |
| `return` (关键球员回归) | 0.08 | 核心球员复出 |
| `major_rotation` | 0.12 | 大幅轮换 |
| `travel_fatigue` | 0.06 | 长途旅行疲劳 |
| `suspension` | 0.15 | 停赛 |
| `lineup_hint` | 0.10 | 首发不确定 |
| `form_change` | 0.08 | 状态变化 |
| `tactical_shift` | 0.06 | 战术调整 |
| `manager_change` | 0.08 | 教练变更 |
| `morale_event` | 0.04 | 士气事件 |
| `schedule_pressure` | 0.04 | 赛程压力 |
| `weather_impact` | 0.04 | 天气影响 |
| `other` | 0.03 | 其他 |

**实际调整量 = max_adjustment × confidence × reliability，上限 0.15**

### 5.3 可用性状态映射

```python
"out" / "suspended":   -15% xG (关键球员) / -10%
"doubtful":            -6% xG
"probable":            -3% xG
"available":           无影响
"likely_start":        +8% xG (关键球员回归)
```

### 5.4 动态信号倍率

基于历史信号准确率 `signal_track_record` 表：

```
accuracy > 0.80:   multiplier = 1.0  (可靠)
accuracy 0.50-0.80: multiplier = 0.8 (中等)
accuracy < 0.50:   multiplier = 0.5  (差)
无历史:            multiplier = 0.7  (保守默认)
```

### 5.5 风险标签生成

系统自动生成以下风险标签：
- `高海拔场地` — 海拔 ≥ 1500m
- `首发不确定` — lineup_hint + confidence < 0.6
- `旅行疲劳` — 长途旅行
- `关键球员缺阵` — 核心球员伤缺
- `天气扰动` — 天气影响
- `情报冲突` — contradiction_risk = high
- `小组赛轮换风险` — 小组赛第3轮
- `旅行距离较长` — travel > 3000km
- `大雨天气` — 降水 > 5mm
- `雨天影响` — 降水 > 1mm
- `强风天气` — 风速 > 40 km/h
- `高温酷暑` — 温度 > 32°C
- `低温条件` — 温度 < 5°C

### 5.6 同步信号调整逻辑

```
1. 从 news_signals 表加载 APPROVED + enters_model=1 的信号
2. 按 team_name 匹配到主队/客队
3. 按 impact_direction (positive/negative) 累加
4. net_home = min(Σpositive - Σnegative, 0.20)
5. new_home = home_prob + net_home × (1 - home_prob)
6. new_away = away_prob + net_away × (1 - away_prob)
7. new_draw = 1.0 - new_home - new_away
8. 确保所有概率 ≥ 0.01，重新归一化
```

---

## 6. 外部数据源

### 6.1 市场赔率校准 (MarketCalibrator)

**文件位置：** `backend/app/services/market_calibrator.py`

**设计原则：**
1. Phase 1: 仅分歧检测，不修改概率 (shadow mode)
2. Phase 2: 最高 25% 市场融合
3. 只存储隐含概率（vig 去除后），不存储原始赔率

**数据提供者优先级：**

| 优先级 | 提供者 | API Key |
|--------|--------|---------|
| 1 | apifootball.com | `APIFOOTBALL_COM_KEY` |
| 2 | API-Sports / api-football.com | `API_FOOTBALL_KEY` |
| 3 | The Odds API (Pinnacle) | `ODDS_API_KEY` |

**Vig 去除方法 (乘法归一化)：**
```
implied_home = 1/odds_home
implied_draw = 1/odds_draw
implied_away = 1/odds_away
total = implied_home + implied_draw + implied_away

fair_home = implied_home / total
fair_draw = implied_draw / total
fair_away = implied_away / total
vig = total - 1.0
```

**分歧检测：**
```python
DIVERGENCE_THRESHOLD = 0.12  # 12pp 分歧触发风险标签

if |model_home - market_home| > 0.12:
    risk_tags.append("模型与市场存在显著分歧 (Xpp)")
    confidence_penalty = min(divergence × 0.5, 0.15)
```

**样本量感知的市场权重：**
```python
MIN_MARKET_BLEND = 0.05
MAX_MARKET_BLEND = 0.25
BLEND_SATURATION = 2000

market_weight = max(MIN, min(MAX, 0.25 - sample_size / 2000))
# 样本越多，市场权重越小
```

**比赛权重阈值：** 仅对 `competition_weight >= 0.82` 的比赛获取赔率

**查询的运动键映射：**
```
World Cup → soccer_fifa_world_cup
Premier League → soccer_epl
La Liga → soccer_spain_la_liga
Bundesliga → soccer_germany_bundesliga
Serie A → soccer_italy_serie_a
Ligue 1 → soccer_france_ligue_one
Champions League → soccer_uefa_champs_league
Europa League → soccer_uefa_europa_league
```

**球队名称模糊匹配：**
- 处理 `&` ↔ `and`
- 移除 `FC`、`AFC` 后缀
- 子串包含匹配

---

### 6.2 天气数据 (WeatherService)

**文件位置：** `backend/app/services/weather_service.py`

**数据源：** Open-Meteo 免费 API (`https://api.open-meteo.com/v1/forecast`)

**获取参数：**
```
hourly: temperature_2m, precipitation, wind_speed_10m,
        relative_humidity_2m, weather_code
forecast_days: 16
```

**天气影响标签阈值：**

| 天气条件 | 阈值 | 标签 |
|----------|------|------|
| 降水 > 5mm | 大雨 | `大雨天气` |
| 降水 > 1mm | 小雨 | `雨天影响` |
| 风速 > 40 km/h | 强风 | `强风天气` |
| 温度 > 32°C | 高温 | `高温酷暑` |
| 温度 < 5°C | 低温 | `低温条件` |

**WC26 场馆坐标数据库（13个场馆）：**
```
MetLife Stadium (纽约):       (40.81, -74.07)
SoFi Stadium (洛杉矶):        (33.95, -118.34)
AT&T Stadium (达拉斯):        (32.75, -97.09)
Levi's Stadium (旧金山):      (37.40, -121.97)
Arrowhead Stadium (堪萨斯城): (39.05, -94.48)
Lincoln Financial (费城):     (39.90, -75.17)
Gillette Stadium (波士顿):    (42.09, -71.26)
Lumen Field (西雅图):         (47.60, -122.33)
BC Place (温哥华):            (49.28, -123.11)
BMO Field (多伦多):           (43.63, -79.42)
Estadio Azteca (墨西哥城):    (19.30, -99.15)
Estadio BBVA (蒙特雷):        (25.67, -100.31)
Estadio Akron (瓜达拉哈拉):   (20.67, -103.47)
```

**欧洲主场映射：**
```
Belgium → King Baudouin Stadium
England → Wembley Stadium
France → Stade de France
Spain → Santiago Bernabéu
Germany → Olympiastadion Berlin
Italy → San Siro
Netherlands → Johan Cruijff Arena
Portugal → Estádio da Luz
```

---

### 6.3 伤病数据 (InjuryDataService)

**文件位置：** `backend/app/services/injury_data.py`

**数据状态类别：**
```
"out" → 确定缺阵 (expected_minutes_delta = -90)
"doubtful" → 出战成疑 (expected_minutes_delta = -45)
"probable" → 大概率出战 (expected_minutes_delta = -15)
"available" → 可用 (expected_minutes_delta = 0)
```

**概率调整因子：**
```python
"out" (key player):     home_win × (1 - 0.15 × confidence)
                        away_win ÷ (1 - 0.15 × confidence)
"doubtful":             × (1 - 0.08 × confidence)
"probable":             × (1 - 0.03 × confidence)
```

**数据源：**
- 本地 JSON 文件 (`data/injuries.json`) 
- 预留: Transfermarkt / FlashScore 爬虫
- 预留: Sportmonks / API-Football 付费 API

---

### 6.4 新闻信号 (NewsSignals)

**来源：** `news_signals` 数据库表

**有效信号要求：**
- `review_status = 'APPROVED'`
- `enters_model = 1`
- `evidence_id IS NOT NULL`

**字段：**
```python
signal_type, impact_direction, confidence,
summary_zh, player_name, claim,
source_reliability, team_id, evidence_id
```

---

## 7. 蒙特卡洛锦标赛模拟

**文件位置：** `backend/app/services/tournament_simulator.py`

**锦标赛结构（2026世界杯 48 队, 104 场比赛）：**

```
Group Stage:     12 groups × 4 teams × 6 matches    = 72 matches
Round of 32:     16 matches
Round of 16:     8 matches
Quarter-finals:  4 matches
Semi-finals:     2 matches
Final:           1 match
Third Place:     1 match (未模拟)
─────────────────────────────────────────────────
Total:           104 matches
```

**小组赛对阵（每组 6 场）：**
```python
(1, 2), (3, 4), (1, 3), (2, 4), (1, 4), (2, 3)
```

**排名规则：** Points → Goal Difference → Goals For

**32强晋级规则：**
- 每组前 2 名（共 24 队）
- 8 个成绩最好的小组第三

**淘汰赛对阵（Round of 32）：**

| 场次 | 主场 | 客场 |
|------|------|------|
| 1-8 | W_A~W_H | 3rd_1~3rd_8 |
| 9-16 | W_I~W_L / RU | 交叉对阵 |

**比赛模拟逻辑：**
```python
# 基于三路概率推导泊松率
base_lam = 1.0 + 0.8 × (home_win_prob - away_win_prob)
base_mu  = 1.0 + 0.8 × (away_win_prob - home_win_prob)
lam = clamp(base_lam, 0.3, 2.5)
mu  = clamp(base_mu,  0.3, 2.5)

# 泊松采样 (带拒绝采样确保高平局概率场景)
hg, ag = poisson(lam), poisson(mu)
if draw_prob > 0.35 and hg != ag: 重采样 (最多 10 次)
```

**淘汰赛点球：** 50/50 硬币翻转

**输出概率列表：**
```
group_win_prob      — 小组第一概率
advance_prob        — 晋级淘汰赛概率
round_of_32_prob    — 进入 32 强概率
round_of_16_prob    — 进入 16 强概率
quarter_final_prob  — 进入 8 强概率
semi_final_prob     — 进入 4 强概率
final_prob          — 进入决赛概率
champion_prob       — 夺冠概率
```

**默认运行次数：** 10,000 次

**概率来源：**
- 从 `wc26_match_predictions` 表加载（如果可用）
- 从 JSON 文件加载
- 手动通过 `set_match_probability()` 设置
- 默认: home_win=0.40, draw=0.30, away_win=0.30 (小组赛)

---

## 8. 评估指标与后处理

### 8.1 Dixon-Coles 评估

```python
def evaluate(test_df):
    # Brier Score: 均方概率误差
    brier_score = mean((probs - actual_onehot)²)

    # Log Loss: 负对数似然
    log_loss = mean(-log(predicted_prob_of_actual))

    # 校准曲线: 10个置信度桶
    for bucket in 0.0-0.1, 0.1-0.2, ..., 0.9-1.0:
        calibration[bucket] = accuracy_in_bucket

    # 比分命中率
    exact_score_hit_rate = 正确预测精确比分的比例
    top3_hit_rate = 正确比分在Top3预测中的比例
```

### 8.2 Skellam 淘汰赛平局修正

**文件位置：** `backend/app/services/skellam.py`

**问题：** 淘汰赛阶段所有泊松类模型系统性低估平局概率 3-5pp

**解决方案：**
```python
DEFAULT_KNOCKOUT_BIAS = 0.03

# 从历史 postmatch_eval 计算实际偏差
bias = mean(actual_draw_i - predicted_draw_i for last 20 knockout matches)
# clamp to [0.01, 0.08]

new_draw = min(old_draw + bias, 0.45)  # 上限 45%
# 按比例分配剩余概率
```

### 8.3 PredictionResult 输出结构

```python
@dataclass
class PredictionResult:
    # 比赛信息
    home_team: str
    away_team: str
    competition: str
    is_neutral: bool

    # 最终概率 (融合后)
    home_win_prob: float
    draw_prob: float
    away_win_prob: float

    # xG
    home_xg: float
    away_xg: float

    # 各层独立概率
    dc_probs: dict        # Dixon-Coles 原始
    enhancer_probs: dict  # Tabular Enhancer
    elo_probs: dict       # κ-Elo Davidson
    pi_probs: dict        # Pi-Rating
    weibull_probs: dict   # Weibull Copula
    market_probs: dict    # 市场隐含概率

    # Elo 详情
    home_elo: float       # 主队 Elo
    away_elo: float       # 客队 Elo
    elo_gap: float        # Elo差

    # 元数据
    top_scores: list      # Top 3 比分
    score_matrix: list    # 5×5 得分矩阵
    weight_config: WeightConfig
    components_used: list
    risk_tags: list
    confidence_penalty: float

    # 市场
    market_applied: bool
    market_weight_used: float
    divergence: float

    # 信号/上下文
    active_events: list
    context_adjustments: list

    # 降级原因
    degraded_reasons: list[DegradedReason]

    # 数据源状态
    source_status: dict[str, SourceStatus]
```

---

## 9. FIFA 排名层级数据

**文件位置：** `backend/app/services/dixon_coles.py` (WC26_FIFA_TIERS)

### 完整 48 队 FIFA 层级映射 (2026年5月近似排名)

**Tier 1 (Top 10):**
```
阿根廷, 巴西, 乌拉圭, 哥伦比亚, 法国, 英格兰, 西班牙, 葡萄牙,
荷兰, 德国, 比利时, 克罗地亚, 意大利
```

**Tier 2 (11-30):**
```
厄瓜多尔, 摩洛哥, 塞内加尔, 埃及, 尼日利亚, 日本, 伊朗,
韩国, 澳大利亚, 美国, 墨西哥, 加拿大, 丹麦, 瑞士, 奥地利,
塞尔维亚, 乌克兰, 瑞典, 土耳其, 波兰
```

**Tier 3 (31-50):**
```
智利, 秘鲁, 巴拉圭, 喀麦隆, 科特迪瓦, 加纳, 突尼斯, 阿尔及利亚,
南非, 马里, 卡塔尔, 沙特阿拉伯, 阿联酋, 乌兹别克斯坦, 伊拉克,
威尔士, 匈牙利, 捷克, 挪威, 苏格兰, 罗马尼亚, 斯洛伐克, 希腊,
哥斯达黎加, 巴拿马, 牙买加
```

**Tier 4 (51+):**
```
玻利维亚, 委内瑞拉, 布基纳法索, 布隆迪, 刚果(金), 几内亚,
中国, 约旦, 阿曼, 巴林, 洪都拉斯, 萨尔瓦多, 新西兰,
塔希提, 斐济, 所罗门群岛, 斯洛文尼亚, 保加利亚, 芬兰,
海地, 库拉索, 佛得角
```

---

## 10. 完整数据流总结

### 10.1 训练数据来源

```
┌─────────────────────────────┐
│  matches + match_results    │
│  + teams (SQLite/PostgreSQL)│
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│  training DataFrame          │
│  列: match_date, home_team,  │
│  away_team, home_goals,      │
│  away_goals,                 │
│  competition_weight,         │
│  is_neutral_venue,           │
│  home_xg, away_xg            │
└─────────────┬───────────────┘
              │
    ┌─────────┼─────────┬──────────┬──────────┐
    ▼         ▼         ▼          ▼          ▼
 DC.fit()  Enh.fit()  Elo.fit()  Pi.fit()  Weibull.fit()
```

### 10.2 单场预测完整流程

```
输入: home_team, away_team, competition, is_neutral, match_date, venue

Step 1:  加载训练数据和模型 (3层缓存: 内存→磁盘→训练)
Step 2:  Dixon-Coles → xG + 得分矩阵 + 基础概率
Step 3:  TabularEnhancer → 36维特征 ML 概率
Step 4:  融合 Step 2 + Step 3 (FusionGraph Step 1)
Step 5:  融合 Weibull (可选, FusionGraph Step 2)
Step 6:  Elo → κ-Elo Davidson 概率 → 融合 (FusionGraph Step 3)
Step 7:  Pi-Rating → 概率 → 融合 (FusionGraph Step 4)
Step 8:  歧义检测 (FusionGraph.compute_disagreement)
Step 9:  归一化 (确保 H+D+A = 1.0)
Step 10: 伤病信号调整 (injury_data)
Step 11: 新闻信号调整 (news_signals)
Step 12: 天气数据获取 (Open-Meteo)
Step 13: 市场赔率获取 (The Odds API / apifootball / API-Sports)
Step 14: 市场校准 (shadow mode 记录，Phase 2 融合)
Step 15: 场地因素 (高海拔 xG 调整)
Step 16: 人工事件调整 (伤病/回归/轮换/旅行)
Step 17: 上下文调整 (中立场地、德比、决赛等)
Step 18: 风险标签汇总 + 置信度惩罚
Step 19: 生成 PredictionResult
Step 20: 保存 PreMatchSnapshot 到数据库
Step 21: 可选 LLM 分析 (DeepSeek V4 Pro)
  ├── 比赛分析文章
  ├── 视频脚本
  └── 社交媒体文案
```

### 10.3 模型缓存策略 (3层)

```
Layer 1: 内存缓存 (ModelCache) — dict in-memory
Layer 2: 磁盘缓存 (model_cache_disk/) — pickle 文件
Layer 3: 冷启动训练 — 从 DataFrame 重新 fit

缓存键: competition + training_df_fingerprint
```

### 10.4 可插拔的数据源矩阵

| 数据源 | 获取方式 | 必须? | 降级策略 |
|--------|----------|-------|----------|
| 训练数据 (SQLite DB) | SQL 查询 | 是 | 抛异常 |
| Dixon-Coles 模型 | 3层缓存 | 是 | 冷启动训练 |
| TabularEnhancer | 3层缓存 | standard+ | 降级为 baseline |
| Elo 评分 | JSON artifact | standard+ | 降级为 baseline |
| Pi-Rating | JSON artifact | full+ | 跳过，标记 degraded |
| Weibull | pickle | research-full | 跳过 |
| 伤病数据 | JSON 文件 | 否 | 跳过，不调整 |
| 新闻信号 | DB 查询 | 否 | 跳过，不调整 |
| 天气 | Open-Meteo API | 否 | 返回 "未知" |
| 市场赔率 | 3个 API 级联 | 否 | 跳过，标记 unavailable |
| LLM 分析 | DeepSeek API | 否 | 返回 degraded |
| 球队元数据 | DB 查询 | 是 | 冷启动默认值 |

### 10.5 关键数值参数速查表

| 参数 | 值 | 用途 |
|------|-----|------|
| Dixon-Coles 半衰期 | 180 天 | 时间衰减权重 |
| Elo 初始评分 | 1500 | 新球队默认 |
| Elo 主场优势 | 100 分 | 非中立场地 |
| Elo K (联赛) | 20 | 常规比赛 |
| Elo K (淘汰赛) | 32 | 世界杯/淘汰赛 |
| κ 默认值 | 0.24 | 平局频率参数 |
| κ EPL | 0.28 | 英超高平局率 |
| κ UCL | 0.18 | 欧冠低平局率 |
| Pi-Rating k | 0.1 | 评分变化速率 |
| Pi PROB_SCALE | 0.35 | 概率映射尺度 |
| Enhancer learning_rate | 0.06 | 梯度提升学习率 |
| Enhancer max_depth | 4 | 树深度 |
| Enhancer max_iter | 220 | 迭代次数 |
| DC params att/def | [0.2, 5.0] | 攻击力/防守力范围 |
| 高海拔阈值 | 1500m | xG × 0.95 |
| 分歧检测阈值 | 12pp | 触发风险标签 |
| 市场最大融合 | 25% | Phase 2 上限 |
| 市场最小融合 | 5% | 有数据时的基准 |
| 市场饱和样本量 | 2000 | 权重降到最小时 |
| 重采样最终归一化阈值 | 0.001 | total≠1时归一化 |
| 信号最大概率偏移 | 0.15 | 硬上限 |
| 信号默认倍率 | 0.7 | 无历史数据时 |
| 蒙特卡洛模拟次数 | 10,000 | 锦标赛模拟 |
| 训练数据半衰期 | 180 天 | 贝叶斯收缩 |
| 冷启动最小比赛 | 5 | 低于此视为冷启动 |
| 天气预测窗口 | 16 天 | Open-Meteo 限制 |
| 比赛权重 (世界杯) | 1.5 | training weight |
| 比赛权重 (友谊赛) | 0.5 | training weight |
| 比赛权重 (默认) | 0.9 | training weight |

---

## 附录：关键代码文件索引

| 文件 | 功能 |
|------|------|
| `backend/app/services/prediction_pipeline.py` | 统一预测入口 |
| `backend/app/services/prediction_core.py` | 离线/artifact 预测核心 |
| `backend/app/services/dixon_coles.py` | DC 泊松模型 + 冷启动 + FIFA 层级 |
| `backend/app/services/tabular_match_model.py` | ML 特征增强器 (36维特征) |
| `backend/app/services/elo_ratings.py` | κ-Elo Davidson 评分系统 |
| `backend/app/services/pi_ratings.py` | Pi-Rating 评分系统 |
| `backend/app/services/weibull_model.py` | Weibull Copula 模型 |
| `backend/app/services/fusion_graph.py` | 顺序加权融合图 |
| `backend/app/services/weights.py` | 权重配置 (比赛类型感知) |
| `backend/app/services/signal_adjuster.py` | 异步信号调整器 |
| `backend/app/services/signal_adjuster_sync.py` | 同步信号调整器 |
| `backend/app/services/market_calibrator.py` | 市场赔率校准 |
| `backend/app/services/weather_service.py` | Open-Meteo 天气服务 |
| `backend/app/services/injury_data.py` | 伤病数据服务 |
| `backend/app/services/skellam.py` | 淘汰赛平局修正 |
| `backend/app/services/tournament_simulator.py` | 蒙特卡洛锦标赛模拟 |
| `backend/app/services/prediction_enhanced.py` | Dashboard/Creator 增强预测 |
| `backend/app/services/prediction_result.py` | 标准化预测结果结构 |
| `backend/app/models/prediction_run.py` | 预测运行 DB 模型 |
| `backend/app/routers/predictions.py` | 预测 API 路由 |
| `backend/scripts/predict_wc26.py` | 命令行预测脚本 |
| `data/team_tournament_status.json` | 48队分组 + 晋级状态数据 |